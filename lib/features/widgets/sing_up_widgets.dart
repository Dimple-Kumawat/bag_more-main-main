import 'package:flutter/material.dart';

class SingUpWidgets extends StatelessWidget {
  final String mid_text;
  final String text_filed;
  final String button;
  const SingUpWidgets(
      {super.key, required this.mid_text, required this.text_filed, required this.button});

  @override
  Widget build(BuildContext context) {
    return const Center(
      
      
    );
  }
}
